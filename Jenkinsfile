node {
    def project = 'jenkins-157313'
    def appName = 'drupal'
    def feSvcName = 'feSvcDrupal'
    def imageTag = "gcr.io/${project}/${appName}:${env.BRANCH_NAME}.${env.BUILD_NUMBER}"

    checkout scm


    switch (env.BRANCH_NAME) {
        case "master":


        break
    }
}